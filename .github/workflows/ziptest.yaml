name: zip-test

on:
  push:
    branches:
      - main

jobs:
  zip_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run Test
        run: |
          zip -X test.zip test.txt
          md5sum test.zip
